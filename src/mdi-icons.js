// Curated MDI path subset — only paths used by HomePanel
// Full library: @mdi/js (7500+ icons). Add here as needed.

export {
  // UI Controls
  mdiCog as settingsPath,
  mdiClose as closePath,
  mdiMinus as minimizePath,
  mdiPlus as addPath,
  mdiPencil as editPath,
  mdiMenu as menuPath,
  mdiDrag as dragHandlePath,
  mdiRefresh as refreshPath,
  mdiMagnify as searchPath,
  mdiChevronDown as expandMorePath,
  mdiChevronUp as expandLessPath,
  mdiCheck as checkPath,
  mdiCheckCircle as checkCirclePath,
  mdiAlert as warningPath,
  mdiAlertCircle as errorPath,
  mdiInformationOutline as infoPath,
  mdiDotsVertical as moreVertPath,

  // Media Controls
  mdiPlay as playPath,
  mdiPause as pausePath,
  mdiSkipNext as skipNextPath,
  mdiSkipPrevious as skipPreviousPath,
  mdiVolumeHigh as volumeUpPath,
  mdiMusicNote as musicNotePath,

  // Connection / Status
  mdiWifi as wifiPath,
  mdiWifiOff as wifiOffPath,
  mdiBell as bellPath,

  // Utility
  mdiFileDocument as fileDocumentPath,
  mdiPower as powerPath,
  mdiBrightness6 as brightnessPath,
  mdiCamera as cameraPath,
  mdiVideo as videocamPath,

  // Entity: Light
  mdiLightbulb as lightbulbOnPath,
  mdiLightbulbOff as lightbulbOffPath,
  mdiLightbulbOutline as lightbulbDimPath,

  // Entity: Switch
  mdiToggleSwitch as switchOnPath,
  mdiToggleSwitchOffOutline as switchOffPath,

  // Entity: Fan
  mdiFan as fanOnPath,
  mdiFanOff as fanOffPath,

  // Entity: Sensors
  mdiThermometer as temperaturePath,
  mdiWaterPercent as humidityPath,
  mdiBattery as batteryPath,
  mdiFlash as powerSensorPath,
  mdiGauge as pressurePath,
  mdiWeatherSunny as illuminancePath,
  mdiChartLine as genericSensorPath,

  // Entity: Binary Sensors
  mdiMotionSensor as motionOnPath,
  mdiMotionSensorOff as motionOffPath,
  mdiDoorOpen as doorOpenPath,
  mdiDoorClosed as doorClosedPath,
  mdiWindowOpenVariant as windowOpenPath,
  mdiWindowClosed as windowClosedPath,
  mdiCheckboxMarked as binarySensorOnPath,
  mdiCheckboxBlankOutline as binarySensorOffPath,
  mdiSmoke as smokePath,
  mdiWaterAlert as moisturePath,
  mdiGarageOpen as garageOpenPath,
  mdiGarage as garageClosedPath,
  mdiLockOpenVariant as lockOpen2Path,

  // Entity: Climate
  mdiThermostat as climatePath,

  // Entity: Media Player
  mdiSpeaker as mediaPlayerPath,
  mdiSpeakerOff as mediaPlayerOffPath,
  mdiTelevision as tvPath,

  // Entity: Camera
  mdiCctv as cctvPath,

  // Entity: Lock
  mdiLock as lockPath,
  mdiLockOpen as lockOpenPath,

  // Entity: Cover / Blinds
  mdiRollerShade as coverOpenPath,
  mdiRollerShadeClosed as coverClosedPath,

  // Entity: Person / Tracker
  mdiHome as homePresencePath,
  mdiAirplane as awayPath,

  // Entity: Scene
  mdiPalette as scenePath,

  // Entity: Automation
  mdiRobot as automationPath,

  // Entity: Alarm
  mdiShieldHome as alarmPath,

  // Entity: Vacuum
  mdiRobotVacuum as vacuumPath,

  // Entity: Timer
  mdiTimer as timerPath,
  mdiTimerOff as timerOffPath,

  // Entity: Input Boolean
  mdiToggleSwitchVariant as inputBoolOnPath,
  mdiToggleSwitchVariantOff as inputBoolOffPath,

  // Entity: Script
  mdiScript as scriptPath,

  // Entity: Input Select / Select
  mdiFormSelect as selectPath,

  // Entity: Number / Input Number
  mdiNumeric as numberPath,

  // Entity: Button
  mdiGestureTapButton as buttonPath,

  // Entity: Update
  mdiUpdate as updatePath,

  // Weather
  mdiWeatherPartlyCloudy as weatherDefaultPath,
  mdiWeatherSunny as weatherSunnyPath,
  mdiWeatherCloudy as weatherCloudyPath,
  mdiWeatherRainy as weatherRainyPath,
  mdiWeatherSnowy as weatherSnowyPath,
  mdiWeatherLightning as weatherThunderstormPath,
  mdiWeatherFog as weatherFogPath,
  mdiWeatherWindy as weatherWindyPath,
  mdiWeatherNight as weatherNightPath,
  mdiWeatherSnowyRainy as weatherSleethPath,
  mdiWeatherHail as weatherHailPath,

  // Fallback
  mdiHelp as unknownPath,
} from '@mdi/js';
